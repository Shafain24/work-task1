semantic version
It is a way to name versions of software so you can understand what changed.

What does ^ mean in version numbers?
it means to install a crtin version or any new version given that new version does not have major changss (i.e code breaking changes).

What's the difference between dependencies and devDependencies?
dependncies are the packages that your code nees to work/run.
dev-dependencis are he packages that you as a devoloper need while developing like testing tools.